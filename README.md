# Personal Progress Tracker & Knowledge Base

Персональный трекер прогресса и база знаний — учебный проект, который помогает одновременно:

- структурировать личное обучение (темы, ресурсы, заметки, прогресс);
- прокачивать навыки Python, ООП, работы с файлами, тестирования;
- переходить от консольного приложения к веб‑API на FastAPI и SQLAlchemy.

Проект развивается по этапам: от простого CLI до полноценного backend‑сервиса.

---

## 🌱 Возможности

### Этап 1 — CLI‑приложение

- Создание **тем для изучения** (например, «Основы Python», «Базы данных»).
- Работа с содержимым темы:
  - добавление **ресурсов** (ссылки, текстовые материалы);
  - добавление **заметок**;
  - отметка **прогресса** (процент выполнения).
- Хранение данных в **JSON‑файле**:
  - сериализация объектов в словари и обратно;
  - безопасная работа с файлами через контекстные менеджеры.
- Простое консольное меню:
  - показать пользователя и количество тем;
  - добавить/просмотреть темы;
  - добавить ресурс/заметку/прогресс для конкретной темы.

### Этап 2 — FastAPI‑API

- REST API поверх доменной модели:
  - `GET /health` — проверка состояния сервиса;
  - `GET /topics` — список тем;
  - `POST /topics` — создание темы;
  - `GET /topics/{title}` — получение темы по названию.
- Pydantic‑схемы для строгой валидации данных и удобной автодокументации.
- Переход к **SQLite + SQLAlchemy** для хранения тем (и дальнейшего расширения на ресурсы/заметки/прогресс).

---

## 🧱 Архитектура и стек

### Стек технологий

- **Язык**: Python 3.12
- **CLI**: стандартная библиотека (`input`, `print`, `json`, `pathlib`)
- **Веб‑фреймворк**: FastAPI
- **Хранилище**:
  - Этап 1: JSON‑файл (`data/data.json`);
  - Этап 2: SQLite (`data/tracker.db`) + SQLAlchemy (ORM).
- **Тестирование**: pytest
- **Типизация**: аннотации типов (`from __future__ import annotations`)

### Структура проекта (упрощённо)

```text
personal_tracker/
├── app/
│   ├── api/
│   │   └── topics.py        # FastAPI роуты для тем
│   ├── db/
│   │   ├── database.py      # engine, SessionLocal, Base, get_db()
│   │   ├── init_db.py       # создание таблиц
│   │   └── models.py        # ORM-модели (TopicModel и др.)
│   ├── schemas/
│   │   └── topic.py         # Pydantic-схемы для API
│   ├── services/
│   │   └── topic_service.py # CRUD-логика для тем
│   └── main.py              # FastAPI-приложение
├── core/
│   └── utils.py             # валидация URL, поиск тем и др. утилиты
├── models/
│   ├── user.py              # User (доменная модель для CLI)
│   ├── topic.py             # Topic + ресурсы/заметки/прогресс (CLI)
│   ├── resource.py
│   ├── note.py
│   └── progress.py
├── storage/
│   └── file_storage.py      # JSON-хранилище с автосохранением (context manager)
├── tests/
│   ├── test_storage.py      # тесты хранилища
│   ├── test_app.py          # тесты CLI
│   └── ...                  # тесты для веб-части
├── data/
│   ├── data.json            # данные для CLI
│   └── tracker.db           # SQLite БД для FastAPI/ORM
└── app.py                   # CLI-приложение
